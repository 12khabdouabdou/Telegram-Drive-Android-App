<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark variant of the app theme. Picked automatically by the
         framework when uiMode is night, or via AppCompatDelegate when
         the user explicitly enables dark mode from Settings. -->
    <style name="Theme.TelegramDrive" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">#FF0E141B</item>
        <item name="android:windowBackground">@color/dark_background</item>
    </style>
</resources>
